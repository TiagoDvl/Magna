CREATE TABLE User (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    legislaturaId TEXT,
    FOREIGN KEY(legislaturaId) REFERENCES Legislatura(id)
);

-- Create
insertUser:
INSERT OR REPLACE INTO User(id, legislaturaId)
VALUES (?, ?);

-- Read all
selectAllUsers:
SELECT * FROM User;

-- Read by ID
selectUserById:
SELECT * FROM User WHERE id = ?;

-- Update
updateUserLegislatura:
UPDATE User
SET legislaturaId = ?
WHERE id = 1;

-- Delete
deleteUserById:
DELETE FROM User WHERE id = ?;